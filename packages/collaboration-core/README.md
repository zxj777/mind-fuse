# @mind-fuse/collaboration-core

Yjs-backed canvas substrate for Mind-Fuse.

- Owns the generic `DocumentManager` and `SpatialGrid`
- Remains product-agnostic and does not define Investigation entities
- Used by `@mind-fuse/investigation` to host domain state alongside canvas shapes
