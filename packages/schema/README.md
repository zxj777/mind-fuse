# @mind-fuse/schema

Import/export boundary for Investigation data.

- Defines the serialized bundle format for investigation documents
- Owns schema versioning for future capture entry points
- Keeps management-surface and external-entry serialization out of app code
