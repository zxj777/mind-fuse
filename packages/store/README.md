# @mind-fuse/store

State container for the investigation workspace.

- Tracks selection, viewport, active snapshot, and suggestion queue
- Keeps workspace interaction state outside of app entry points
- Is intentionally UI-framework-light and domain-aware only through package APIs
