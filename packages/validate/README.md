# @mind-fuse/validate

Runtime validation layer for the investigation workspace.

- Validates canvas-binding invariants
- Encodes the allowed conclusion status transitions
- Validates `AISuggestion` payloads before they reach the domain manager
